#!/usr/bin/env bash

set -e

cp bin/nginx "$1/bin/"
cp bin/start-nginx "$1/bin/"
cp bin/start-unicorn "$1/bin/"
echo '-----> Installed NGINX 1.4.1'

mkdir -p "$1/config"
cp config/mime.types "$1/config/"
cp config/nginx.conf.erb "$1/config/"
echo '-----> NGINX config copied to app/config/'

exit 0
